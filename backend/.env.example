# Copie para .env e ajuste. NUNCA versione o .env real.

APP_NAME="WFM Segurança Patrimonial"
ENV=development

# PoC (SQLite). Produção (VPS Hostinger):
# DATABASE_URL=postgresql+psycopg2://wfm:senha@localhost:5432/wfm
DATABASE_URL=sqlite:///./wfm.db

# Gere com: openssl rand -hex 32
SECRET_KEY=troque-isto-em-producao

# Homem-morto. Para TESTAR rápido, use 1 minuto:
DEADMAN_INTERVAL_MINUTES=30
DEADMAN_JITTER_MINUTES=5
DEADMAN_RESPONSE_WINDOW_SECONDS=120

# Geofence / anti fake-gps
DEFAULT_GEOFENCE_RADIUS_M=100
MAX_PLAUSIBLE_SPEED_MPS=30

# #4 Gate de qualidade do GPS
MAX_GPS_ACCURACY_M=100
MAX_FIX_AGE_SECONDS=120

# #5 Cruzamento GPS x IP (requer geoip2 + base GeoLite2)
GEOIP_ENABLED=false
GEOIP_DB_PATH=
GEOIP_MAX_DIVERGENCE_KM=200

# #8 Janela de turno (exige check-in dentro da escala)
REQUIRE_SHIFT_WINDOW=false
SHIFT_GRACE_MINUTES=30

# Matcher facial: stub | deepface | serpro_datavalid | aws_rekognition
LIVENESS_PROVIDER=stub
FACE_MATCH_THRESHOLD=0.90
LIVENESS_THRESHOLD=0.80
AWS_REGION=sa-east-1

# DeepFace (matcher self-hosted, R$0/consulta). Requer: pip install deepface
DEEPFACE_MODEL=ArcFace
DEEPFACE_DETECTOR=opencv

# Liveness ATIVA por desafio de movimento (validada no servidor via MediaPipe)
REQUIRE_LIVENESS_CHALLENGE=true
LIVENESS_ACTIONS=blink,turn_left,turn_right,smile
LIVENESS_ACTIONS_PER_CHALLENGE=2
LIVENESS_CHALLENGE_WINDOW_SECONDS=30

# Serpro Datavalid (validação facial contra a base oficial do governo).
# Credenciais na Área do Cliente do Serpro. Para ativar: LIVENESS_PROVIDER=serpro_datavalid
DATAVALID_BASE_URL=https://gateway.apiserpro.serpro.gov.br
DATAVALID_TOKEN_PATH=/token
DATAVALID_FACE_PATH=/datavalid/v4/pf-facial
DATAVALID_CONSUMER_KEY=
DATAVALID_CONSUMER_SECRET=

# Webhook da central (homem-morto perdido / pânico)
CENTRAL_WEBHOOK_URL=

CORS_ORIGINS=*
