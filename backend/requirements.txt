fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic[email]==2.10.4
pydantic-settings==2.7.1
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.0.1
python-multipart==0.0.20
apscheduler==3.11.0
httpx==0.28.1
# Matcher facial self-hosted (LIVENESS_PROVIDER=deepface). Pesado (~torch/tf):
# deepface==0.0.93
# pillow==11.0.0

# Cruzamento GPS x IP (#5, GEOIP_ENABLED=true). Requer base GeoLite2 (MaxMind):
# geoip2==4.8.0

# Produção (VPS Hostinger): descomente para PostgreSQL + PostGIS
# psycopg2-binary==2.9.10
# geoalchemy2==0.16.0
# celery[redis]==5.4.0
